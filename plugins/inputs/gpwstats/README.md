# Gpwstats Input Plugin

## Configuration

```toml
# Gather packets and bytes counters from gpwstats
  [[inputs.gpwstats]]
    ## The default timeout of 1s for ipset execution can be overridden here:
    # timeout = "1s"

```
